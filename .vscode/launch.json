{
  "configurations": [
    {
      "name": "Launch via Bun",
      "request": "launch",
      "env": {
        "NODE_ENV": "dev",
        "DB_ENV": "prod",
      },
      "program": "${workspaceFolder}/src/application/app.ts",
      "type": "bun"
    }
  ]
}