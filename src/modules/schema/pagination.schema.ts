export interface PageQueryDto {
  // page: current page.
  page: number;
  size: number;
}
export interface PaginateResponseDto {
  count: number;
  // page: current page.
  page: number;
  size: number;
}
